#include "main.h"

/**
 * print_line - a function that draws a stratight line in the terminal
 * followed by a new line
 * @n: An integer
 * Return: Always 0
 */

void print_line(int n)
{
	int 1 = 0;

	if (n > 0)
	{
		for (; i < n; i++)
		{
			_putchar('_');
		}
	}
	_putchar('\n');
}
